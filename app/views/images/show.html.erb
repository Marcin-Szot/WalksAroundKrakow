<!DOCTYPE html>
<html lang="pl" xmlns="http://www.w3.org/1999/html">
<head>
  <meta charset="utf-8" />
  <title>Spacery po Krakowie</title>
  <script type="text/javascript">
       $(document).ready(function(){
           $("html").css({
               background: "black"
           });
           $("body").css({
               background: "black"
           });
       });
  </script>
</head>
<body>
      <div class = "doc">
        <div id = "image_content">
          <%= link_to image_tag(@image.photo_url.to_s), post_path(@post) %>
        </div>

        <div id = "photo_title">
          <%= @image.title %>
        </div>

        <div id = "photo_description">
          <%= @image.description %>
        </div>
      </div>
</body>